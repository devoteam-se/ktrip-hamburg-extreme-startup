# Kotlin (Spring) boilerplate for Extreme Startup

## Run
* To start, run `./gradlew bootRun`.

### Run with hot reload
* Open up two terminals
* In the first one, run `./gradlew build --continuous`
* In the second one, run `./gradlew bootRun`

## Requirements

* Java 17 or higher
